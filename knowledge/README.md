# База знаний для RAG

Добавьте сюда документы для аналитики: методологии, кейсы, шаблоны.

Поддерживаемые форматы: `.txt`, `.md`, `.pdf`, `.docx`.

После добавления или изменения файлов запустите индексацию:

```bash
python -m scripts.build_index
```
